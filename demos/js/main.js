$(function(){


	$("#slidemap-horizontal").slideMap({
		debug 			: false,
	});

	$("#slidemap-3d").slideMap({
		debug 			: true,
		useMaskShadow 	: false,
	});

	// $("#slidemap-captions").slideMap({
	// 	debug 			: false,
	// 	showShadow 		: true,
	// });

});